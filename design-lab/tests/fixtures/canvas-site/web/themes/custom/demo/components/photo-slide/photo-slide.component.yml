name: Photo slide
description: One photograph in a carousel.
group: Media
props:
  type: object
  required:
    - image
  properties:
    image:
      type: object
      title: Image
    fit:
      type: string
      title: Fit
      enum:
        - cover
        - contain
      default: cover
