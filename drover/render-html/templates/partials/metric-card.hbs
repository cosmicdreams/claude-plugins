<article class="metric-card{{#if modifier}} metric-card--{{modifier}}{{/if}}">
  <p class="metric-card__label">{{label}}</p>
  <p class="metric-card__value">{{value}}</p>
  {{#if hint}}<p class="metric-card__hint">{{hint}}</p>{{/if}}
</article>
